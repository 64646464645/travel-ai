---
alwaysApply: false
scene: service_verification
---

# 服务关闭规范

改完后端代码并通过验收后，必须关闭所有已启动的服务（开发服务器、构建进程等），释放所占端口，避免端口占用（如 `EADDRINUSE`）。

## 要点

- 验收完成后，停止 `npm run dev` 及任何手动启动的服务进程。
- 确认端口（默认 3300）不再被占用后再结束任务。
- 如需重启服务，先结束占用端口的旧进程，避免新旧进程端口竞争。
