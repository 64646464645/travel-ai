# 规则上下文按需加载优化 - 检查清单

## 开发前

- [x] 已确认当前 Rule 的实际加载方式、front matter 语法和可用场景值。
- [x] 已定位核心规则、专项规则、`AGENTS.md` 与现有规则配置中的重复内容。
- [x] 已确认不修改业务代码、数据库和环境变量。

## 开发中

- [x] T1 已完成：规则加载矩阵草案包含全部现有 Rule。
- [x] T2 已完成：新增核心规则，并收敛 `AGENTS.md` 的重复约束。
- [x] T3 已完成：专项规则具备按需加载元数据或等效配置。
- [x] T4 已完成：完整与轻量 Spec 流程的边界、例外和回退条件已明确。
- [x] T5 已完成：规则加载矩阵已固化并与 Rule 内容一致。
- [x] T6 已完成：简单任务和数据库任务样本均已核对。

## 完成后验收

- [x] 仅 `core.md` 被声明为常驻规则，且其内容仅覆盖跨任务的最小约束和专项规则路由提示。
- [x] 每份专项规则都具有可复核的触发场景或按需加载说明，且不再无条件注入所有任务。
- [x] `AGENTS.md` 不再重复核心规则或专项规则中的完整内容，保留项目结构、命令和必要导航信息。
- [x] Spec 工作流明确区分完整 Spec 适用范围和可直接执行的简单任务范围。
- [x] 简单任务的轻量流程仍要求安全检查和与风险匹配的验证，不降低数据库、环境或 API 变更的治理要求。
- [x] 规则加载矩阵完整列出核心与专项规则的状态、触发条件和职责。
- [x] 通过静态检查确认简单任务不会要求读取无关的数据库或提交规则，数据库变更仍要求迁移规则。
- [x] 修改后的文档不包含真实密钥、用户数据或无关业务改动。
